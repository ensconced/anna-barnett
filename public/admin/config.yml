backend:
  name: git-gateway
  branch: master

media_folder: "public/media"
public_folder: "/media"

collections:
  - label: "Sections"
    name: "sections"
    folder: "site/sections"
    fields:
      - label: "Body"
        name: "body"
        widget: "markdown"
